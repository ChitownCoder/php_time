<?php

$a = 10;

// $b = 5;
$b = 10.00;

$c = TRUE;

$d = FALSE;

// this is an if Statement

if ($a > $b) { // if this is true 
    echo "0. TRUE"; // then this will be printed
} else { // if not
    echo "0. FALSE"; // then this will be printed
}

// this is an if Statement is this equal to
echo "<br>";

if ($a == $b) { // if this is true 
    echo "1. TRUE"; // then this will be printed
} else { // if not
    echo "1. FALSE"; // then this will be printed
}

// this is an if Statement is - this is not equal to
echo "<br>";

if ($a != $b) { // if this is true 
    echo "2. TRUE"; // then this will be printed
} else { // if not
    echo "2. FALSE"; // then this will be printed
}

// this is an if Statement is 
echo "<br>";

if ($c) { // if this is true 
    echo "3. TRUE"; // then this will be printed
} else { // if not
    echo "3. FALSE"; // then this will be printed
}

// this is an if Statement is 
echo "<br>";

if ($a === $b) { // if this is true 
    echo "4. TRUE"; // then this will be printed
} else { // if not
    echo "4. FALSE"; // then this will be printed
}
